function Display({ display }) {
	return (
		<div className='flex items-center justify-center text-white text-center border border-red-400 rounded-full m-auto h-60 w-60'>
			<p>{display}</p>
		</div>
	);
}

export default Display;
